# Лабораторная работа №1 (Часть 2: Модульное тестирование)

**Выполнил:** Волынчук Максим Сергеевич, группа АС-577

## Описание задания
В рамках второго задания были написаны модульные тесты для программы, моделирующей температуру объекта управления (разработанной в Задании 1). В качестве фреймворка для тестирования использовался Google Test.

## Структура проекта
* `src/` — содержит исходный код программы (логика математических моделей вынесена в файлы `model.h` и `model.cpp` для удобства тестирования).
* `test/` — содержит файл `test_model.cpp` с написанными модульными тестами.

## Результаты тестирования и покрытие кода (Coverage)
Были разработаны тесты, покрывающие базовые сценарии расчета для обеих математических моделей (линейной и нелинейной).

* **Количество написанных тестов:** 2 (по одному на каждую функцию-модель `simulateLinear` и `simulateNonlinear`).
* **Процент покрытия кода (Code Coverage):** 100% 

Тесты успешно проходятся, подтверждая корректность выполнения расчетов согласно заданным формулам и начальным условиям. Анализ покрытия проводился с помощью утилиты `gcovr`.